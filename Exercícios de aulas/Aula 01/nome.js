const nome = 'Thiago'
const idade = '15'

if (idade >=18){
    console.log (nome + ' tem ' + idade + ' anos, logo, já atingiu a maioridade.')
}else{
    console.log (nome + ' tem ' + idade + ' anos, logo, ainda não atingiu a maioridade.')
}